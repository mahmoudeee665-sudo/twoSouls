name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase project
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            "https://bgfmlaawqkldjxuztfyw.supabase.co/rest/v1/memories?select=count" \
            -H "apikey: sb_publishable_AGIzngMZ121kB4oLhWICeg_koN3su9h"
